/**
 * Theme layer barrel.
 *
 * Typed Design_Tokens and Themes applied via the `data-theme` root attribute
 * (Requirement 4). Import `./tokens.css` once at the app entry point to register
 * the CSS Custom Properties for every Theme.
 */
export * from './tokens';
